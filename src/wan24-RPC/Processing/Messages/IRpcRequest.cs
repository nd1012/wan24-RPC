namespace wan24.RPC.Processing.Messages
{
    /// <summary>
    /// Interface for a RPC request message (which can be used with a <see cref="RpcProcessor.Request"/> or a <see cref="RpcProcessor.Call"/>)
    /// </summary>
    public interface IRpcRequest : IRpcMessage
    {
    }
}
